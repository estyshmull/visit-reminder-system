# Frontend - Development environment variables
# הערות: קובץ זה מיועד להרצה מקומית של ה-frontend

# כתובת ה-API שה-frontend יקרא אליה במהלך פיתוח
VITE_API_URL="http://localhost:3000/api"

# שם האפליקציה (להצגה בכותרת, בפאווטר וכו')
VITE_APP_NAME="מערכת תזכורות ביקורים (פיתוח)"

# הערות:
# - אין להכניס סודות ל-frontend; כל סוד צריך להישאר ב-backend או ב-secret manager.
# - לשימוש ב-HTTPS בפיתוח ניתן להגדיר פרוקסי או להשתמש ב-ngrok/localhost.run
