# Frontend - Production environment variables
# הערות: values אלה נטענים בזמן בניית ה-frontend או בסביבת ה-host

VITE_API_URL="https://api.your-domain.com/api"
VITE_APP_NAME="מערכת תזכורות ביקורים"

# Security notes:
# - ה-frontend לא אמור להכיל סודות רגישים. כל מפתחות/סודות חייבים להיות מאוחסנים ב-backend.
# - ודא שה-API מוגן באמצעי אימות (JWT / session) לפני חשיפת פעולות רגישות.
